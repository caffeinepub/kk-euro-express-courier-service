{
  "kind": "implementation_plan",
  "version": "1.0",
  "title": "Replace homepage video fallback asset with newly uploaded office image",
  "requirements": [
    {
      "id": "REQ-38",
      "summary": "Swap the Home page VideoSection fallback image while keeping the public URL `/assets/video-fallback.jpeg` unchanged.",
      "acceptanceCriteria": [
        "The frontend contains a static asset at `frontend/public/assets/video-fallback.jpeg` whose content matches the newly uploaded `image-1.png` (converted/renamed as needed).",
        "When `/assets/watch-our-video.mp4` fails to load, `frontend/src/components/site/VideoSection.tsx` continues to render `<img src=\"/assets/video-fallback.jpeg\" ...>` and the displayed image is the newly uploaded one.",
        "The public URL used by the site for the fallback image remains exactly `/assets/video-fallback.jpeg` (no route changes and no backend involvement).",
        "No changes are made to the Internet Identity/auth hooks or other immutable frontend paths."
      ],
      "file_operations": [
        {
          "path": "frontend/public/assets/video-fallback.jpeg",
          "operation": "modify",
          "description": "Replace the existing fallback image file contents with the newly uploaded `image-1.png` (convert to JPEG as needed) while keeping the filename and path exactly `video-fallback.jpeg` so the public URL remains `/assets/video-fallback.jpeg`."
        }
      ]
    }
  ]
}